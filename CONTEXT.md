# CONTEXT.md — dsh-skill-injector 领域术语表

> 本文件只收录领域术语与精确定义，不含实现细节。需求见 `docs/需求/全局.md`。
> 术语如有冲突或歧义，以此处定义为准；发现新术语即时补录。

## 术语

### 技能（skill）
dsh 中可被激活的指令化能力，用户以 `/grill-me` 形式触发；激活后在当前会话内持续生效。插件只关心其**名称**与**用户可调用性**，不关心技能内部实现。

### 预设组（Preset Group）
一组**有序**技能名称的集合，是配置与选择的基本单位。字段：名称（唯一）、描述（可选）、技能列表（有序、至少 1 个）、启用（布尔）。
与"单个技能"是**整体-成员**关系；与"组合（多个组叠加）"是互斥的——V1 为单选模型，多组叠加属畅想。

### 预设选择器（Preset Selector）
输入区工具栏上的按钮 + 弹出菜单，是用户与"当前预设组"交互的唯一入口：查看已启用组、选中、反选。位于**权限选择器**之后。

### 权限选择器（Permission Select）
dsh 原生组件（输入区左下角第二个按钮，点击可见 Full Access）。预设选择器只**紧邻**它，绝不修改它；两者样式必须一致（同主题共变）。

### 当前预设组（Active Preset Group）
用户当前选中的预设组；未选中任何组时**不存在**（不是"空组"）。选中状态全局持久化，直到**反选**或该组被删除/禁用（自动反选）。

### 反选（Deselect）
对当前选中组再次点击，取消选择。反选是"停止自动填充"的唯一手段，语义与"切换"不同：切换是选中另一个组，反选是回到无选择状态。

### 自动填充（Auto-fill）
当前预设组存在时，插件把技能文本（一行、空格分隔、如 `/grill-me /domain-modeling`）自动填入输入框**首行**的行为。填充对象是输入框文本，**不是**直接激活技能，也不是自动发送。

### 填充守卫（Fill Guard）
填充前的检查规则：输入框去掉所有空白后为空 → 允许填充；否则不填充。目的是**保护已有内容**——填充从不修改非空输入框。

### 有效内容（Effective Content）
输入框去掉所有空白（含换行）后剩余的文本。非空即有有效内容，此时填充被守卫拦截。注意与"内容"区分：只有空白字符的输入框没有有效内容。

### 启用 / 禁用（Enabled / Disabled）
预设组的管理开关。启用 = 出现在选择器菜单中；禁用 = 隐藏且不可被选中（若恰好是当前预设组则自动反选）。与技能本身是否可调用无关——那是 dsh 技能系统的事。

### 全局持久化（Global Persistence）
"当前预设组"状态跨会话、跨刷新保持的存储行为，直到用户反选。注意与"会话级激活"区分：技能激活是会话级的（dsh 原生），预设组选择是全局的（本插件）。
